For Streaming:

To run the file ->

Open one terminal and run the command -> "python3 stream.py -f spam -b 2"

Open another terminal and run the command -> /opt/spark/bin/spark-submit streamer.py

You can find the dataframe in the forEach function of streamer. Use that dataframe 
to apply preprocessing or models.
